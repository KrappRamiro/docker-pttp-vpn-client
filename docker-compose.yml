services:
  pptp-vpn-client:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      VPN_SERVER: ${VPN_SERVER}
      VPN_USER: ${VPN_USER}
      VPN_PASS: ${VPN_PASS}
    privileged: true
